server:
  host: "0.0.0.0"
  port: 8000
  workers: 1
  reload: false

# Public URL that external users' browsers should call to reach the API.
# Example: https://api.your-domain.com or http://your-server:8000
public_base_url: "http://localhost:8000"